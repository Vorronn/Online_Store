const initialState = {
    favoriteCount: 4
}

const favoriteReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default favoriteReducer;
